import { AccountValidatorDirective } from './account-validator.directive';

describe('AccountValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
